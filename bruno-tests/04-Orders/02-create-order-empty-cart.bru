meta {
  name: Create Order - Empty Cart
  type: http
  seq: 2
}

post {
  url: http://localhost:8080/api/orders
  body: json
  auth: bearer
}

auth:bearer {
  token: {{aliceToken}}
}

body:json {
  {
    "items": []
  }
}

assert {
  res.status: eq 400
}
