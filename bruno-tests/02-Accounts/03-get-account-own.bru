meta {
  name: Get Account - Own
  type: http
  seq: 3
}

get {
  url: http://localhost:8080/api/accounts/{{aliceId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{aliceToken}}
}

assert {
  res.status: eq 200
  res.body.username: eq Alice
}
