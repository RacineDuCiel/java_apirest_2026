meta {
  name: Delete Account - Admin
  type: http
  seq: 8
}

delete {
  url: http://localhost:8080/api/accounts/{{bobId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

assert {
  res.status: eq 200
}
