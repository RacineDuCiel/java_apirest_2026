meta {
  name: 09 - Login Wrong Password (should fail)
  type: http
  seq: 9
}

post {
  url: http://localhost:8080/api/auth/login
  body: json
  auth: none
}

body:json {
  {
    "username": "alice_paris",
    "password": "WrongPassword"
  }
}

assert {
  res.status: eq 401
}
