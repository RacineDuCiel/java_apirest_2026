meta {
  name: 16 - Delete My Account (USER)
  type: http
  seq: 16
}

delete {
  url: http://localhost:8080/api/accounts/4
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token_charlie}}
}

assert {
  res.status: eq 200
  res.body: eq "Compte supprimé avec succès"
}
