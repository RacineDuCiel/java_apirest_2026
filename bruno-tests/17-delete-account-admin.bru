meta {
  name: 20 - Delete Account ADMIN (should succeed)
  type: http
  seq: 20
}

delete {
  url: http://localhost:8080/api/accounts/3
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token_admin}}
}

assert {
  res.status: in [200, 204]
}
